apiVersion: v1
kind: Service
metadata:
  name: calendar-shell
  labels:
    app: calendar-shell
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: calendar-shell
