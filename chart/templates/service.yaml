apiVersion: v1
kind: Service
metadata:
  name: calendar
  labels:
    app: calendar
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: calendar
