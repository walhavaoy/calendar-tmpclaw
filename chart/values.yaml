replicaCount: 1

image:
  repository: localhost:31500/calendar-tmpclaw/calendar
  tag: latest
  pullPolicy: Always

service:
  port: 8080

resources: {}

env:
  PGHOST: postgres
  PGPORT: "5432"
  PGDATABASE: calendar
  PGUSER: calendar
  PGPASSWORD: calendar
